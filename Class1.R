## Class 1 -- 2022/09/06 ##
# 1. Basic calculation (+, -, *, /, ^, sqrt(), logb())
# 2. ()(round brackets)、[](square, bracket)、{}(curly) in R
# 3. Space in R
# 4. Tidy or clean code
# 5. what is function
# 6. How to open help file of the function
# 7. Rules of feeding the arguments
# 8. Method and rules to assign a object, assignment sign and equal sign (<- and =)
# 9. How to use comment (#)
# 10. Remove the variable, rm()
# 11. How to use object
# 12. Uppercase and lowercase in R
# 13. Single quote and double quote (" and ')
# 14. Logical value and logical vector. Logical operator.
# 15. Vector, matrix, data.frame and list 
  # 15-1. https://www.davidzeleny.net/wiki/lib/exe/fetch.php/recol:materials:vector_matrix_data.frame_list_v1.1.pdf
  # 15-2. class()
# 16. Detail of vector
  # 16-1. c()
  # 16-2. seq(from, to, by)
  # 16-3. Number vector and character vector
  # 16-4. length()
  # 16-5. nchar() length of the string
  # 16-6. How to use :(colon)
  # 16-7. How to subset specific value from vector ([1:5], [c(1,5)])
# 17. Detail of Matrix
  # 17-1. Vector and matrix can only have one data type
  # 17-2. How to generate a new matrix: matrix(data, nrow, ncol, byrow)
  # 17-3. view(matrix)
  # 17-4. colnames()
  # 17-5. How to subset the matrix
    # 17-5-1. Using [] and c() ([5, c(2,3)])
    # 17-5-2. Using column name (, c("b", "c"))
# 18. Detail of dataframe
  # 18-1. Data for example: trees, LETTERS, letters
  # 18-2. How to subset the dataframe
    # 18-2-1. Using [] and () ([5, c(2,3)])
    # 18-2-2. Using column name (, c("b", "c"))
    # 18-2-3. Using $ sign
  # 18-3. Modify the dataframe
    # 18-3-1. Add new column: cbind()
  # 18-4. Each column can be different class
# 19. Detail of list
  # 19-1. How to generate a new list: list()
  # 19-2. How to subset the list using $, c() and []
  # 19-3. Each element can be different class